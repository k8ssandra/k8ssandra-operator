apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
patches:
  - target:
      kind: K8ssandraCluster
    patch: |-
      - op: add
        path: /spec/cassandra/networking
        value: { hostNetwork: true }
