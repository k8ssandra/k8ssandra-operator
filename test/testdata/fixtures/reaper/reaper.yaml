apiVersion: reaper.k8ssandra.io/v1alpha1
kind: Reaper
metadata:
  name: reaper1
spec:
  datacenterRef:
    # TODO check multi-namespace when cluster-scope is ready
    name: dc1
  cassandraUserSecretRef:
    name: test-cql-secret
  jmxUserSecretRef:
    name: test-jmx-secret
  reaperUiSecretRef:
    name: test-ui-secret
  containerImage:
    repository: thelastpickle
    name: cassandra-reaper
    tag: "3.0.0"
    pullPolicy: Always
  initContainerImage:
    repository: thelastpickle
    name: cassandra-reaper
    tag: "3.0.0"
    pullPolicy: IfNotPresent
