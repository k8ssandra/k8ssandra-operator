apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands: 
- script: |- 
    sed "s/serverVersion: .*/serverVersion: ${SERVERVERSION:-4.0.1}/g" '../../testdata/samples/k8ssandra-single-kind.yaml' | kubectl apply -n $NAMESPACE --server-side -f - 
  ignoreFailure: false 